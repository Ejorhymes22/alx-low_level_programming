#include <stdio.h>
/**
 * main - prints with printf
 *
 * Return: success (0)
 */

int main(void)
{
	char s[] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", s);
	return (0);
}
